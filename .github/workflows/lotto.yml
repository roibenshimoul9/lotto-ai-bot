name: Lotto Weekly AI

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * 1"  # Monday (UTC)
    - cron: "0 6 * * 5"  # Friday (UTC)

jobs:
  run-job:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Run Lotto Script
        run: node src/job.js
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_GROUP_ID: ${{ secrets.TELEGRAM_GROUP_ID }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

      # 🔥 חדש – commit אם CSV השתנה
      - name: Commit updated lotto CSV if changed
        run: |
          git config user.name "lotto-bot"
          git config user.email "bot@github.com"

          if git diff --quiet; then
            echo "No changes to commit"
          else
            git add .
            git commit -m "🎰 Auto-update lotto results"
            git push
          fi
